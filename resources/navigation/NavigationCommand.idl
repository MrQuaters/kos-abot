/* Package with an "IConfiguration interface declaration. */

package echo.NavigationCommand

struct NavigationCommandData {
    UInt8 command;
    UInt32 durationMs;
    SInt8 speed;
}

interface {
    /* Ping method interface. */
    SetNavigationCommand(in NavigationCommandData value, out UInt8 result);
}
